@page "/giftcards/settings"
@attribute [Authorize]
@inject NavigationManager Navigation
@code { protected override void OnInitialized() => Navigation.NavigateTo("/config?section=giftcards", replace: true); }
